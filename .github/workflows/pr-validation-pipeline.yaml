name: PR Validation Pipeline.

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  pr-pipeline:
    name: "PR Validation Pipeline"
    uses: MohsinNaseem/reusable-workflows/.github/workflows/pr-validation-pipeline.yaml@main
    with:
      environment: ${{ github.event.inputs.environment }}
      main-repo: ${{ github.event.repository.name }}
    secrets: inherit